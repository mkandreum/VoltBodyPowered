<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen background uses near-black to match VoltBody theme -->
    <style name="Theme.VoltBody.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#09090C</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_volt</item>
        <item name="windowSplashScreenBrandingImage">@drawable/ic_volt</item>
        <item name="postSplashScreenTheme">@style/Theme.VoltBody</item>
    </style>

    <style name="Theme.VoltBody" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
